## -*- mode:ulam -*-
#=t41675_test_compiler_localvardeclwithfunccalsamename_ish
#!
Exit status: 64
Ue_A { constant Unsigned cFIX_BITS = 6u;  Int m_m(64);  Int test() {  Int m = self ( )m . cast 6u << cast;  self m_m . m = m return } }
##
## informed by Dave ish 9/14/24; see also errors t41674, t3797, t41486.
##  okay to have a function in initialization of local variable with same name.
##
#>A.ulam
  ulam 5;
element A {
  constant Unsigned cFIX_BITS = 6u;
  Int m_m = 1;
  Int m() { return m_m; }
  Int test() {
    Int m = (Int) (((Bits) m()) << cFIX_BITS);
    m_m = m;
    return m;
  }
}
#.
