## -*- mode:ulam -*-
#=t41524_test_compiler_elementmultiinheritance_shodowingtypedefsinhierarchy
##
## gen output: (+ interleaved asserts)
##  Unsigned Arg: 3
##  Unsigned Arg: 1
##
#!
Exit status: 1
Uq_Moo { typedef Moo Mine;  Unsigned(3) m_us( 4u);  <NOMAIN> }
Ue_Foo : Bar +Moo +Bar { typedef Bar Moo;  Bool(7) sp(true);  :Bar< typedef Bar Mine;  Unsigned(3) m_bus(3u); > +Moo< typedef Moo Mine;  Unsigned(3) m_us(4u); > +Bar< typedef Bar Mine;  Unsigned(3) m_bus(3u); > Int test() {  System s;  Bar f;  Unsigned a = f ( )func . cast;  self sp . a 3u cast == cast = s ( a )print . s ( self sp . cast )assert . f m_bus . 1u cast = s ( f m_bus . cast )print . s ( f m_bus . 1u cast == )assert . f m_bus . cast return } }
Uq_Bar { typedef Bar Mine;  Unsigned(3) m_bus( 3u);  <NOMAIN> }
Uq_System { <NOMAIN> }
##
## shadow typedefs across bases ok; breadth-first order search; see also t41525;
## not sure about the copies of same base, shared, but gen COMPONENTS in .h fooled.
##
#>Moo.ulam
 ulam 3;
quark Moo {
  typedef Moo Mine;
  Unsigned(3) m_us = 4u;
  Unsigned(3) func()
  {
    return m_us;
  }
}

#:Foo.ulam
  ulam 5;
element Foo : Moo + local.Moo + Bar{
  Bool(7) sp;
  typedef Bar Moo; //wait for it..2 Bars allowed?
  Int test() {
    System s;
    Mine f;
    Unsigned a = f.func(); //breadth-first
    sp = (a == 3u);
    s.print(a);
    s.assert(sp);
    f.m_bus = 1u;
    s.print(f.m_bus);
    s.assert(f.m_bus==1u);
    return (Int) f.m_bus;
  }
}

#:Bar.ulam
  ulam 3;
quark Bar {
  typedef Bar Mine;
  Unsigned(3) m_bus = 3u;
  Unsigned(3) func()
  {
    return m_bus;
  }
}

#:System.ulam
  ulam 1;
quark System {
  Void print(Unsigned arg) native;
  Void print(Int arg) native;
  Void print(Int(4) arg) native;
  Void print(Int(3) arg) native;
  Void print(Unary(3) arg) native;
  Void print(Bool(3) arg) native;
  Void assert(Bool b) native;
}
#.
