## -*- mode:ulam -*-
#=t3726_test_compiler_transientinstanceof_funccallarg
#!
##      gen code output: (2 asserts)
##
##        unevaluable without assuming atoms as elements (c should be false)
##
Exit status: 0
Ue_A : QW { :<> Bool(1) b(true);  Bool(1) c(true);  Int(32) test() {  System s;  b ( self.instanceof )func = s ( b )assert . Atom(96) a;  c ( a )func = s ( c ! )assert . 0 cast return } }
Uq_System { <NOMAIN> }
Uq_QW { <NOMAIN> }
##
## Dave requested a way to get default values for Self (within a quark);
## a loophole, since a quark would be an undefined atom, but as a ref, its done (t3679)
##
  ## similar to 3685, except a transient
##
#>R.ulam
  ulam 2;
use System;
use A;
element R {
  Bool b, c;
  Int test(){
    System s;
    A at;
    b = at.func(A.instanceof);
    s.assert(b);
    return 0;
  }
}

#:A.ulam
  ulam 2;
use QW;
transient A : QW {
  Bool(7) sp;
}

#:QW.ulam
  ulam 2;
transient QW {
  Bool func(A aarg)
  {
    return (aarg is Self);
  }
}

#:System.ulam
ulam 1;
quark System {
Void print(Unsigned arg) native;
Void print(Int arg) native;
Void print(Int(4) arg) native;
Void print(Int(3) arg) native;
Void print(Unary(3) arg) native;
Void print(Bool(3) arg) native;
Void assert(Bool b) native;
}
#.
