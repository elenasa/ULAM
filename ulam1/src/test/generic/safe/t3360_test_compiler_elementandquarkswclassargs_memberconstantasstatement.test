## -*- mode:ulam -*-
#=t3360_test_compiler_elementandquarkswclassargs_memberconstantasstatement
#!
Exit status: 2
Ue_Foo { Int(32) test() {  Booly(0u,2u,true) b;  b 7u . b 2u . cast return } }
Uq_Booly(Unsigned(3) firstRange, Unsigned(3) lastRange, Bool(3) bomb) { <NOMAIN> }
##
##      //informed by issue (see 3359): also tests Bool as 3rd class parameter
##      // b.bomb has NodeMemberSelect as parent, not NodeStatements event though its a "solo" statement
##
#>Foo.ulam
ulam 1;
use Booly;
element Foo{
Int test(){
Booly(0u, 2u, true) b;
b.bomb;
 return b.lastRange;
}
}

#:Booly.ulam
quark Booly(Unsigned(3) firstRange, Unsigned(3) lastRange, Bool(3) bomb) {
Bool behave(){
 if(bomb && lastRange != firstRange) return true;
return false;
}
}
#.
