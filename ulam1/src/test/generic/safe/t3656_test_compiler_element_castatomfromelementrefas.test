## -*- mode:ulam -*-
#=t3656_test_compiler_element_castatomfromelementrefas
#!
Exit status: 1
Ue_Foo : QPerc { :< Bool(1) b(true); > Int(32) test() {  Atom(96) a = self cast;  { a Foo as cond { Foo& a;  a b . true = self.storageof a.storageof = 1 cast return } if } 0 cast return } }
Uq_QPerc { Bool(1) b(false);  <NOMAIN> }
##
## informed by t3655
##
#>Foo.ulam
  ulam 2;
use QPerc;
element Foo : QPerc {

  Int test() {
    Atom a = self;
    //if(a as QPerc)
    if(a as Self)
    {
      a.b = true;
      self.storageof = a.storageof; //(Atom) a;
      return 1;
    }
    return 0;
  }
}

#:QPerc.ulam
  ulam 2;
quark QPerc {
  Bool b;
}

#.
