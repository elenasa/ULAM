## -*- mode:ulam -*-
#=t3701_test_compiler_elementandquark_castclassreftoatomref_error
#!
./A.ulam:11:5: ERROR: Reference variable aref's type Atom(96)&, and its initial value type A&, are incompatible.
./A.ulam:4:9: ERROR: 1 Nodes with erroneous types detected after type labeling class: A.
Unrecoverable Program Type Label FAILURE.
##
## informed by t3697
##
#>A.ulam
  ulam 2;
use System;
use QW;
element A : QW {
  Bool c;
  Int test(){
    System s;
    Qbase& qref = self;
    qref.m_five = 0x3;

    Atom & aref = self; //ERROR for NOW.
    //aref = (Atom&) qref.storageof; //atom ref assignment
    aref = qref.storageof; //atom ref assignment
    qref.m_five = 0x5;
    A apple = (A) aref;
    c = (apple.m_five == 0x5);
    s.assert(c);

    return (Int) c;
  }
}

#:QW.ulam
  ulam 2;
use Qbase;
quark QW : Qbase{
  Bool bw;
  Self func()
  {
    Atom a = self.storageof;
    //Self& sra = (Self&) a; //new way!
    Self& sra = (Self) a; //immediate quark with current values
    return sra;
  }
}

#:Qbase.ulam
  ulam 2;
quark Qbase{
  Unary(5) m_five;
}

#:System.ulam
ulam 1;
quark System {
Void print(Unsigned arg) native;
Void print(Int arg) native;
Void print(Int(4) arg) native;
Void print(Int(3) arg) native;
Void print(Unary(3) arg) native;
Void print(Bool(3) arg) native;
Void assert(Bool b) native;
}
#.
